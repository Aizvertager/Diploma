const showPopupPrice = () => {

    const priceBtn = document.querySelectorAll('.discount-btn'),
        checkBtn = document.querySelector('.check-btn');
    console.log('priceBtn: ', priceBtn);
    console.log(checkBtn);

};

export default showPopupPrice;